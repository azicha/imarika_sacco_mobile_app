import 'package:flutter/material.dart';

class FullStatementPage extends StatelessWidget {
  const FullStatementPage({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}